{
  "name": "vdux-{{basename}}",
  "description": "{{description}}",
  "repository": "git://github.com/vdux-components/{{basename}}.git",
  "version": "0.1.0",{{#mit}}
  "license": "MIT",{{/mit}}
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-core": "^6.5.2",
    "babel-plugin-transform-react-jsx": "^6.5.2",
    "babel-preset-es2015": "^6.1.2",
    "babel-preset-stage-2": "^6.5.0",
    "babel-tape-runner": "^2.0.1",
    "babelify": "^7.2.0",
    "tap-dev-tool": "^1.3.0",
    "hihat": "^2.6.4",
    "tape": "^4.2.0",
    "vdux": "^2.3.1"
  }
}
